apiVersion: apps/v1
kind: Deployment
metadata:
  name: snake-deployment-test
  labels:
    app: greedy-snake
spec:
  replicas: 2
  minReadySeconds: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: greedy-snake
  template:
    metadata:
      labels:
        app: greedy-snake
    spec:
      containers:
        - name: greedy-snake
          image: hkccr.ccs.tencentyun.com/tke_cluster/tke_cluster
          imagePullPolicy: Always
          ports:
            - containerPort: 8888
              protocol: UDP
      imagePullSecrets:
        - name: regcred
      restartPolicy: Always
      nodeSelector:
        stage: test